
buildAll = function() {

  // loop through the data referenced in the settings_lite.json to find the charts + images that should be built.
  for (i = 0; i < _settings['parameters'].length; i++) {
    console.log('buildAll!')
  }
}
